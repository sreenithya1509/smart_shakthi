{
  "name": "smart-shakthi",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered realtime women safety and emergency response platform.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "keywords": [
    "women-safety",
    "sos",
    "react",
    "express",
    "socket.io",
    "mongodb"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
